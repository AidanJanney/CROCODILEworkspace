# CrocoGallery notebook IDs rendered into $BASK_PATH/workspace/ when
# --notebooks is passed to install.sh (and included by --all).
# One ID per line. Blank lines and lines starting with '#' are ignored.
# List available IDs with: crocogallery template --list-notebooks
crocodash.tutorial
crocodash.projects.ocean_only
